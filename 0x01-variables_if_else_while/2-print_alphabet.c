#include <stdio.h>
#include <stdlib.h>

/**
 * main - A program that prints alphabet with function
 * Return: Always 0 (success)
 */

int main(void)
{
	char ab;
	char ac;

	for (ab = 'a'; ab <= 'z'; ab++)
	{
		putchar(ab);
	}

	putchar('\n');

	return (0);
}

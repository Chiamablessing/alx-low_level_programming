include "main.h"
#include <ctype.h>

/**
 *_isupper - checks uppercase letter
 *@c: variable type int
 *Return: always 0
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z'))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
